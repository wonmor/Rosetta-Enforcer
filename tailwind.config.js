module.exports = {
    content: [
        "./_site/*.{html,js}",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}

// To do: add a favicon file to ./images => convert the PNG file to ICO => code written on top of the base.njk file